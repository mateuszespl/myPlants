import styled from "styled-components";

export const StyledCarousel = styled.section`
  .swiper-container {
    position: relative;
    width: 100%;
  }

  .swiper-wrapper {
    height: 100vh;
    width: 100%;
    position: relative;
  }
`;
